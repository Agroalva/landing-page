{
  "name": "landing-page-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "build": "turbo run build",
    "build:app:production": "pnpm --filter app build:production",
    "submit:app:ios": "pnpm --filter app submit:ios",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start": "turbo run start",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.49.0",
    "turbo": "^2.3.0"
  }
}
